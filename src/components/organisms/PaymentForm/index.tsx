import { PaymentFormContainer } from "./styles";

const PaymentForm = () => {
  return (
    <PaymentFormContainer>
      <h3>Formulário de Pagamento</h3>
      <p>Integração com Stripe será configurada aqui.</p>
    </PaymentFormContainer>
  );
};

export default PaymentForm;
